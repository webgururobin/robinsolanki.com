import styled from "styled-components"

export const ImgButton = styled.img`
  width: 6rem;
  cursor: pointer;
  transition: all 0.3s ease;
  margin-right: 2rem;

  :hover {
    transform: scale(1.1);
  }
`
